var React = require('react');

var WeatherMessage = React.createClass({
  render: function () {
    return (
      <p>The weather in Oakland is hot.</p>
    );
  }
});

module.exports = WeatherMessage;
